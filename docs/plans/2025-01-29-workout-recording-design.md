# Workout Recording Design

## Overview

Автоматическая запись тренировок в JSON с ручным контролем через уведомления при паузе.

## Решения

- **Старт/стоп**: автоматический (activity-based)
- **Частота записи**: каждую секунду
- **Формат**: JSON (конвертер в FIT/TCX позже)
- **UI истории**: нет (только сохранение файлов)

## Структура данных

### Файл тренировки

```json
{
  "id": "2024-01-29T18-30-00",
  "startTime": "2024-01-29T18:30:00Z",
  "endTime": "2024-01-29T19:15:00Z",
  "summary": {
    "duration": 2700,
    "distance": 15.2,
    "avgPower": 165,
    "maxPower": 320,
    "avgHeartRate": 142,
    "maxHeartRate": 175,
    "avgCadence": 85,
    "avgSpeed": 28.5
  },
  "samples": [
    {"t": 0, "power": 150, "hr": 120, "cadence": 80, "speed": 25.0},
    {"t": 1, "power": 155, "hr": 122, "cadence": 82, "speed": 25.5}
  ]
}
```

### Расположение файлов

- macOS: `~/Library/Application Support/com.notchrider.app/workouts/`
- Имя: `2024-01-29T18-30-00.json`

## Автоматическое определение тренировки

- **Старт**: `power > 0` или `cadence > 0`
- **Пауза**: 30 сек неактивности → показать уведомление
- **Автостоп**: 5 мин неактивности → автосохранение
- **Минимум**: < 60 сек → автоотмена без вопросов

## UI при паузе

### Уведомление
```
⏸ [P]родолжить [S]охранить [X]тменить
```

### Подтверждения
- **S** → "Сохранить? [Y/N]"
- **X** → "Отменить? Данные потеряны [Y/N]"

### Поведение
- **P** или начать крутить → продолжить запись
- **S + Y** → сохранить файл
- **X + Y** → удалить данные

## Состояния (state machine)

```
[idle] ──активность──► [recording] ──30с──► [paused]
                            ▲                   │
                            └───активность──────┘

[paused] ──S+Y──► [saved] ──► [idle]
[paused] ──X+Y──► [cancelled] ──► [idle]
[paused] ──5мин──► [auto-saved] ──► [idle]
```

## UI в состояниях

| Состояние | Левая часть | Правая часть |
|-----------|-------------|--------------|
| idle | ♥ -- ⚡ 0W 0rpm | 0.0km/h 0.00km 0:00 |
| recording | ♥ 142 ⚡ 165W 85rpm | 28.5km/h 5.23km **12:34** 🔴 |
| paused | **⏸ [P] [S] [X]** | данные |
| confirm-save | **Сохранить? [Y/N]** | |
| confirm-cancel | **Отменить? [Y/N]** | |

Индикатор 🔴 мигает при записи.

## Архитектура

```
Frontend (React)                    Backend (Rust)
─────────────────                   ──────────────
useWorkout.ts                       lib.rs
├─ samples[]                        ├─ save_workout(data)
├─ isRecording                      └─ get_workouts_dir()
├─ isPaused
├─ state machine
└─ UI (уведомления)
```

### Хук useWorkout

```typescript
const {
  isRecording,
  isPaused,
  duration,
  samples,
  save,
  cancel,
} = useWorkout(trainerData);
```

### Tauri команда

```rust
#[tauri::command]
fn save_workout(workout: WorkoutData) -> Result<String, String>
```

## Обработка ошибок

- **Ошибка записи**: показать сообщение, данные в памяти, можно retry
- **Потеря ANT+**: продолжать запись с нулями, через 30с пауза
- **Закрытие приложения**: автосохранение если > 60 сек
- **Краш**: данные теряются (future: периодическое сохранение)

## Расчёт summary

```typescript
summary = {
  duration: samples.length,
  distance: последний sample.distance,
  avgPower: среднее(power, без нулей),
  maxPower: max(power),
  avgHeartRate: среднее(hr, без нулей),
  maxHeartRate: max(hr),
  avgCadence: среднее(cadence, без нулей),
  avgSpeed: distance / (duration / 3600)
}
```
